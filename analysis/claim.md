# Claim

How discoverable and how executable are CHAP's two model-integration routes for an agent working only from public material? One published model is taken down each route -- chapkit_ghr_model as a chapkit model service, minimalist_example_uv as an MLproject/uv model -- until each produces a CHAP evaluation on the public Lao admin-1 monthly data, and what that took is recorded as it happens.

## Children

kind: sub-analyses
main-path: -

## Environment

inherits: the project main environment (`environment/`)

## Answers

_(What this node's analysis yielded. Each answer belongs in the claim collection
under `Human-AI-collaboration/claims/` with a pointer to the result grounding it.)_
