disease_cases ~ 1 + rainfall.rsum3.l1 + mean_temperature.rmean3.l1 + f(seasonal_id, model = 'rw1', cyclic = TRUE, scale.model = TRUE, hyper = prec1) + f(year_id, model = 'iid', constr = TRUE, hyper = prec1)\n\nSampling 1000 draws from chap \nWrote 51 rows x 1000 samp
