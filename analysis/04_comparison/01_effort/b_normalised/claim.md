# Claim

Apply two rules to both logs alike before counting, so that a difference in how two agents coded the same act does not read as a difference between the routes. Rule one attributes a failure to the command that failed, wherever the agent recorded it. Rule two counts effort only up to the moment the route first worked.

## Children

kind: -
main-path: -

## Environment

inherits: the project main environment (`environment/`)

## Answers

_(What this node's analysis yielded. Each answer belongs in the claim collection
under `Human-AI-collaboration/claims/` with a pointer to the result grounding it.)_
