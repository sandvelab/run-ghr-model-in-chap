# Provenance — hierarchical report

One section per generation. Append; never overwrite an existing section.

## 2026-09-21 — first generation

- **What it is**: the linked drill-down over the claim tree (Rule 8). Every node's claim,
  answers, results, scripts, provenance and `run.sh` are one click from its parent, down to
  the raw files; alternatives are marked main-path or not-taken.
- **Generated by**: `.venv/bin/python AI-internal/useful-scripts/build_hierarchical_report.py`
  (`sha256:dffea2eddc9d073bcf2758192025c2f8c2b529ffc44765f708843ce9330bf8d9`)
- **Tree at**: commit `f1daae5`, covering `analysis/` with nodes `01_anchors`,
  `02_routeA_chapkit`, `03_routeB_mlproject`, `04_comparison` (with the `01_effort`
  alternatives node and its two children) and `05_repeatability`.
- **Not tracked**: the generated HTML is gitignored; this file is the record that it was
  generated and from what. Regenerate with the command above after any structural change to
  the tree and before release.
- **Known incompleteness at this generation**: `05_repeatability` was mid-run when this was
  built, so its `run_ledger.tsv`, `completion.tsv` and `score_spread.tsv` are absent or
  empty. Regenerate once that batch closes.
- **Agency**: `agent-autonomous`.
