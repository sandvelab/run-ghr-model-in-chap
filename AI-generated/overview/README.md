# overview

The project's front door: one document that says what was asked, what was done, what came out,
and where the detail is. It exists because the batch reports and the hierarchical report
between them answer every question except "where do I start".

- `26-09-22_overviewV1.md` — version 1, covering the work through batch 7.

**One overview, not several.** When it goes out of date it is superseded by a new version in
this folder, and the old version stays. It is not a place for parallel summaries of the same
material: `readme-at-start.md` says what the project is, the plan holds the decisions, the
batch reports hold the detail, and the hierarchical report is the drill-down. This file points
at those rather than restating them.

Hand-written, unlike most of `AI-generated/`: no re-run produces it, so it is never pruned.
