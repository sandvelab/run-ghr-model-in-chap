# Provenance — model-case-c

One section per file. Append; never overwrite an existing section.

## repo/

- **What it is**: `zlilu/minimal_template_example`, a CHAP `MLproject` template implementing
  Ridge regression with a configurable `alpha`.
- **Obtained by**: `git clone https://github.com/zlilu/minimal_template_example` on
  2026-09-23, pinned at commit `9cbc84a4364a13be8b4c3b809bdd07c4324306c2`. The `.git`
  directory was removed after recording the commit, because a nested repository inside
  `Archive/` is invisible to this repository's version control.
- **Why it is here**: named by the human as a third case. Route A's model declares eighteen
  options that cannot be set through CHAP; route B's model declares none. Neither can
  demonstrate a configuration arriving, which is the claim under test.
- **Licence**: **none declared**. Blocks redistribution of this copy, not analysis.
- **Agency**: `human-pointed` (the repository); `agent-retrieved` (the commit and the clone).
