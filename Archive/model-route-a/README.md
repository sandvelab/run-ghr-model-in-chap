# model-route-a — chapkit_ghr_model

The modelling-resource anchor for route A: a chapkit-based model service,
`chap-models/chapkit_ghr_model`, GPL-3.0, pinned at commit `60b16a2`.

- `repo/` — the repository as published, `.git` removed so this repository versions it.
- `git-metadata.txt` — remote, commit, commit date, subject, clone time.
- `provenance.md` — source, pin, licence, and how it may be used.

Run as published. Not modified, re-specified or tuned (plan §3). The discovering agent for
this route works from the public repository itself; this copy fixes what was run.
