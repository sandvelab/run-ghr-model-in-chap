# model-route-a-it3 — chapkit_ghr_model, re-pinned for iteration 3
(IS_SHADOW)

Route A's modelling-resource anchor for iteration 3 (route A only, against the upgraded
chapkit): `chap-models/chapkit_ghr_model`, GPL-3.0, pinned at commit `a9532c7` (v0.1.2).
Iteration 2's pin, `60b16a2`, stays at `../model-route-a/`.

- `repo/` — the repository as published, `.git` removed.
- `git-metadata.txt` — remote, commit, date, subject, clone time, and the three commits since `60b16a2`.
- `diff-since-it2-pin.patch`, `diff-stat-since-it2-pin.txt` — what upstream changed between the two pins.
- `provenance.md` — source, pin, licence, how it may be used.
