# model-route-b — minimalist_example_uv

The modelling-resource anchor for route B: an `MLproject`-declared model run with `uv`,
`dhis2-chap/minimalist_example_uv`, pinned at commit `5cd8a12`.

- `repo/` — the repository as published, `.git` removed so this repository versions it.
- `git-metadata.txt` — remote, commit, commit date, subject, clone time.
- `provenance.md` — source, pin, and the licence gap.

**No licence is declared upstream.** Redistribution of this copy is not established; it is
raised with the human before release. Run as published, not modified (plan §3).
